# Aumenta o timeout para evitar "context deadline exceeded" em redes lentas
fetch-timeout=120000
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-retries=3

# Suprime avisos de deprecação de dependências transitivas
loglevel=error
